# Read by the Python app (app/config.py) — used inside the backend & bot
# containers. install.sh generates the real backend/.env — this is just
# documentation of what it needs.
DATABASE_URL=postgresql+asyncpg://zanjan:change-me@db:5432/zanjan_campaign
TELEGRAM_BOT_TOKEN=
SECRET_KEY=
