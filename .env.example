# Read by docker-compose.yml itself (compose-level variable substitution).
# install.sh generates the real .env next to docker-compose.yml — this file
# is just documentation of what it needs.
DOMAIN=campaign.behshoor-olive.ir
POSTGRES_PASSWORD=change-me
