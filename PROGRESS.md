# PROGRESS.md

## فاز ۰ — اسکلت پروژه و دیتابیس ✅ (این فاز)
- ساختار پروژه (`backend/`)، `CLAUDE.md`، این فایل
- مدل‌های SQLAlchemy کامل بر اساس منطق تأییدشده (نواحی، مغازه‌ها، تحویل پک، پیگیری سفارش، تحویل پخش، سرکشی، برنامه‌ی روزها، تنظیمات، دفتر کمیسیون)
- Alembic + مایگریشن اولیه (دستی، بدون نیاز به دیتابیس در حال اجرا)
- `docker-compose.yml` (Postgres + جای بک‌اند)، `.env.example`

**برای تست این فاز:**
```bash
cd backend
cp ../.env.example .env   # مقادیر واقعی را پر کن
pip install -r requirements.txt
docker compose up -d db
alembic upgrade head
```
اگر مایگریشن بدون خطا اجرا شد و جدول‌ها در Postgres ساخته شدند، فاز تمام است.

## فاز ۱ — بات تلگرام (در‌آور) ✅
- `app/bot/` — aiogram v3: منوی اصلی، نمایش ناحیه‌ی فعال + عکس مرجع، فرم کامل ثبت مغازه (نام→آدرس→عکس→امتیاز→لوکیشن)، ویرایش هرکدام از آدرس‌های امروز فیلد به فیلد
- «ناحیه‌ی فعال» = نزدیک‌ترین روز پخش (امروز یا بعد) که ناحیه‌اش تعیین شده — همون منطق یک‌روز-جلوتر
- بدون قفل سخت ۶ عصر؛ فقط `editable_until` روی مغازه ذخیره می‌شود (برای نمایش بعدی در وب‌سایت)
- دسترسی بات محدود به نقش‌های SCOUT/ADMIN با `telegram_id` ثبت‌شده (`AuthMiddleware`)
- `scripts/seed_demo_data.py` — چون پنل ادمین هنوز نیست، دیتای تستی (ناحیه‌ها + یک در‌آور + برنامه‌ی فردا) رو دستی می‌سازد
- تست‌شده: import کامل ماژول بات + ساخت Dispatcher بدون خطا (بدون دیتابیس/توکن واقعی). تست end-to-end واقعی نیاز به یک توکن بات واقعی و Postgres دارد — پایین توضیح داده شده.

**برای تست واقعی این فاز:**
```bash
cd backend
docker compose -f ../docker-compose.yml up -d db
cp ../.env.example .env   # TELEGRAM_BOT_TOKEN را از @BotFather بگیر و پر کن
pip install -r requirements.txt
alembic upgrade head
python scripts/seed_demo_data.py --scout-telegram-id <آیدی عددی تلگرام خودت> --scout-name "تست"
python -m app.bot.main
```
بعد با همون اکانت تلگرامی که آیدیش رو دادی، به بات پیام بده و `/start` بزن.

## فاز ۲ — بک‌اند API (FastAPI) ✅
- `app/services/common.py` + `app/services/campaign.py` — کل ماشین‌حالت مغازه و حساب کمیسیون، یک‌جا (هم بات هم API از همین‌جا استفاده می‌کنن، بدون تکرار منطق)
- `app/security.py` — هش رمز (bcrypt) + JWT
- `app/api/` — روترهای FastAPI: `auth`, `regions`, `day_schedules`, `shops`, `pack_deliveries`, `followups`, `fulfillments`, `checkups`, `settings`, `commissions`, `users`
- ماشین‌حالت نهایی (ساده‌تر از سند اولیه؛ چون بین دو رویداد پی‌درپی بدون بازیگر جداگانه، حالت واسط بی‌فایده بود):
  `registered → awaiting_threshold/queued_for_pack → pack_delivered → order1_pending_delivery → order1_done → order2_pending_delivery → steady_customer`
  («pack_delivered» و «order1_done» دقیقاً همون «منتظر پیگیری سفارش بعدی»ان — نیازی به وضعیت جدا نبود)
- صف پخش پک: هر بار محاسبه می‌شه (نه یک‌بار در ابتدای روز) تا آدرس اضطراری همون لحظه وارد لیست شه؛ تقسیم ۵۰-۵۰ روی `Shop.assigned_subteam` ذخیره می‌شه
- رد شدن پیگیری بدون سقف تکرار؛ پیش‌فرض بازگشت ۳ روز بعد اگر ادمین/ویزیتور روز دیگه‌ای ندن
- مبنای کمیسیون = ثبت پخش (نه ثبت ویزیتور)؛ سرکشی هفتگی بعد از مشتری دائمی خودکار زمان‌بندی می‌شه و هر بار تکمیل شد، هفته‌ی بعد رو خودش می‌سازه

**تست‌شده (end-to-end واقعی، نه فقط import):** یک اسکریپت موقت کل مسیر رو از طریق HTTP واقعی (httpx + ASGI، روی SQLite موقت) امتحان کرد: ورود هر سه نقش، ثبت پک، پیگیری سفارش ۱، تحویل پخش، پیگیری سفارش ۲، تحویل پخش، رسیدن به مشتری دائمی، زمان‌بندی سرکشی، رد شدن+بازگشت ۳روزه، و جمع دقیق کمیسیون‌ها. همه‌ی ۲۱ چک پاس شد.

**برای اجرای واقعی این فاز:**
```bash
cd backend
uvicorn app.main:app --reload
# مستندات تعاملی: http://localhost:8000/docs
```
اولین ادمین رو باید مستقیم توی دیتابیس یا با یه اسکریپت seed مثل seed_demo_data.py بسازید (فعلاً endpoint ثبت‌نام عمومی نداریم — عمداً، چون فقط ادمین باید کاربر بسازه).

## فاز ۳ — وب‌اپ با نقشه ✅
- بدون Node/بیلد — HTML/CSS/JS خام + Leaflet و Leaflet.draw از CDN (چون روی این سیستم Node نصب نیست؛ اگه بعداً خواستید می‌شه به React/Vite هم مهاجرت کرد)
- `webapp/index.html` — ورود مشترک، بر اساس نقش ریدایرکت می‌کنه
- `webapp/admin.html` — داشبورد+نقشه (پین مغازه‌ها رنگی بر اساس وضعیت)، نواحی (رسم/ویرایش مرز روی OpenStreetMap)، برنامه‌ی روزها (تاریخ **شمسی**، قابل جابه‌جایی/تعطیلی)، تنظیمات، کاربران، کمیسیون‌ها
- `webapp/visitor.html` — صف پخش پک (با ضبط ویس مرورگر) + صف پیگیری (بستن سفارش با نام محصول / رد کردن با دلیل و تاریخ بازگشت)
- `webapp/distributor.html` — صف تحویل سفارش (ثبت مبلغ) + صف سرکشی هفتگی (تکمیل با روش/توضیح/سفارش احتمالی)
- شماره تلفن مغازه: به‌جای یک فیلد مخصوص ادمین، حالا یه **لیست** است — هر کسی از هر نقشی می‌تونه شماره‌ی جدید با یه توضیح کوتاه اضافه کنه (`ShopPhone` مدل جدید، `js/api.js`'s `phonesSectionHtml`/`wirePhoneSections` مشترک بین هر سه صفحه)
- عکس مغازه (Telegram file_id) از طریق `/shops/{id}/photo` پروکسی می‌شه تا توکن بات هیچ‌وقت به مرورگر نره
- آپلود ویس ویزیتور از طریق `/uploads` (فایل روی دیسک، بدون S3)

**باگ واقعی که پیدا و رفع شد:** نقشه‌ی داخل تب «نواحی» چون در بارگذاری صفحه داخل یه تب مخفی (`display:none`) ساخته می‌شد، سایزش صفر می‌موند و کل محاسبات پیکسل↔مختصاتش خراب بود (رسم و نمایش مرز کار نمی‌کرد). راه‌حل: `map.invalidateSize()` هر بار که اون تب فعال می‌شه.

**تست‌شده (کاملاً end-to-end، با کلیک واقعی روی UI + کراس‌چک از طریق API):** ورود هر سه نقش، رسم و ذخیره‌ی مرز یه ناحیه، افزودن شماره تلفن با توضیح، ثبت تحویل پک از پنل ویزیتور، بستن سفارش ۱ از صف پیگیری، ثبت تحویل و وصول پول از پنل پخش (با محاسبه‌ی درست کمیسیون ۸٪ و ۳٪)، و صف خالی سرکشی (چون هنوز مشتری دائمی‌ای نداریم).

**برای اجرای این فاز:**
```bash
cd backend && uvicorn app.main:app --reload   # یک ترمینال
python -m http.server 5500 --directory ../webapp   # ترمینال دوم
```
بعد `http://localhost:5500` رو باز کن.

## فاز ۴ — داشبورد آمار پیشرفته ✅
- `GET /stats/regions` — یه ردیف به‌ازای هر ناحیه: کل مغازه‌ها، تبلیغ‌شده، در پیگیری، سفارش ۱ داده، سفارش ۲ داده، مشتری دائمی، کنارگذاشته‌شده (تجمیع سمت دیتابیس با `GROUP BY`، نه لوپ توی پایتون)
- تب جدید «آمار نواحی» توی پنل ادمین، همون اول لیست تب‌ها بعد از داشبورد
- تست‌شده با کلیک واقعی روی تب + داده‌ی واقعی از تست‌های قبلی (۵ مغازه‌ی ناحیه ۱ درست تفکیک شدن: ۳ تبلیغ‌شده، ۲ در پیگیری، ۲ سفارش ۱)

## فاز ۵ — دیپلوی ✅
- **ریپو:** https://github.com/MHBehzadian/behshoor-campaign (عمومی — چون نصب تک‌خطی `curl | bash` بدون توکن نیاز به دسترسی به فایل خام گیت‌هاب داره؛ تست شد که `raw.githubusercontent.com/.../install.sh` واقعاً در دسترسه)
- **دامنه:** `campaign.behshoor-olive.ir` (پیش‌فرض قابل تغییر حین نصب) — باید قبل از نصب رکورد A‌ش به IP سرور اشاره کنه
- **نصب تک‌خطی:**
  ```bash
  curl -fsSL https://raw.githubusercontent.com/MHBehzadian/behshoor-campaign/main/install.sh | bash
  ```
  Docker رو اگه نبود نصب می‌کنه، ریپو رو کلون می‌کنه، چندتا سوال می‌پرسه (دامنه، توکن بات، آیدی تلگرام برای بکاپ، یوزر/پس ادمین)، کل استک رو بالا میاره، مایگریشن می‌زنه، ادمین اول رو می‌سازه، و کرون بکاپ رو ست می‌کنه. اجرای دوباره‌ش هم امنه (idempotent).
- **SSL خودکار:** Caddy (`deploy/Caddyfile`) با ACME/Let's Encrypt، بدون هیچ تنظیم دستی — فقط DNS باید از قبل درست باشه.
- **معماری:** `db` (Postgres) + `backend` (FastAPI) + `bot` (همون ایمیج، دستور aiogram) + `caddy` (SSL + پروکسی `/api` + سرو استاتیک `webapp/`)
- **بکاپ کامل (نه فقط دیتابیس):** `scripts/backup.sh` هر ۶ ساعت (کرون) یه آرشیو شامل دیتابیس + فایل‌های آپلودی (ویس‌ها) + خود `backend/.env` و `.env` (توکن بات، رمز دیتابیس، کلید امنیتی) می‌سازه و از طریق بات به آیدی تلگرام ادمین می‌فرسته + آخرین ۳۰ نسخه رو لوکال هم نگه می‌داره — هدف اینه که اگه کل سرور از دست بره، همین یه فایل + ریپو برای بازسازی کامل کافی باشه
- **بازیابی:** `scripts/restore.sh <آرشیو بکاپ>` — بک‌اند/بات رو موقتاً متوقف، دیتابیس و آپلودها رو جایگزین، با تأیید دستی قبل از اجرا؛ بازیابی `.env`ها اختیاریه (فقط موقع بالا آوردن روی سرور تازه لازمه). راهنمای کامل دو حالت (همون سرور / سرور تازه) توی `README.md`
- **تست‌شده:** `docker-compose.yml` (YAML معتبر) و هر سه اسکریپت شل (`install.sh`, `backup.sh`, `restore.sh`) با `bash -n` سینتکس‌چک شدن. اجرای واقعی روی سرور نیاز به یک VPS واقعی + DNS داره که این‌جا در دسترس نبود.

---
منطق کامل کسب‌وکار: https://claude.ai/code/artifact/54c6e469-ffc4-436e-9875-e0dc2c5fb09e
