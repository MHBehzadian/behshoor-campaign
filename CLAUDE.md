# CLAUDE.md — Project Contract

**کمپین بازاریابی به‌شور** — سیستم مدیریت کمپین تبلیغاتی حضوری برند به‌شور در زنجان (نسبت به [behshoor-olive](https://github.com/MHBehzadian/behshoor-olive) یه پروژه‌ی جداست). زنجان به ۲۰ ناحیه تقسیم شده؛ هر روز یک ناحیه هدف انتخاب می‌شود، یک تیم پک رایگان پخش می‌کند، بعد سفارش پیگیری و تحویل می‌شود.

- **ریپو:** https://github.com/MHBehzadian/behshoor-campaign (خصوصی)
- **سایت:** `https://campaign.behshoor-olive.ir` (زیردامنه‌ی behshoor-olive.ir — قابل تغییر حین نصب)
- **نصب:** `curl -fsSL https://raw.githubusercontent.com/MHBehzadian/behshoor-campaign/main/install.sh | bash`

منطق کامل کسب‌وکار (نقش‌ها، چرخه‌ی روزها، حساب و کمیسیون) این‌جا هست:
https://claude.ai/code/artifact/54c6e469-ffc4-436e-9875-e0dc2c5fb09e
این فایل فقط خلاصه‌ی عملیاتی است؛ برای هر ابهام به آرتیفکت بالا رجوع کن، نه برعکس.

## Resume protocol
**IMPORTANT: در شروع هر سشن، اول `PROGRESS.md` را بخوان، بعد ادامه بده.** بعد از اتمام هر فاز، `PROGRESS.md` را آپدیت کن.

## Golden rules
- **`.env` یا هیچ secret ای کامیت نشود** (در `.gitignore` هست).
- ساخت **فاز به فاز** طبق `PROGRESS.md`؛ یک فاز کامل و تست‌شده، بعد فاز بعد.
- متن‌های کاربر و مستندات: **فارسی**. کد، نام متغیر/تابع و کامنت‌ها: **انگلیسی**.
- **بات تلگرام فقط برای نقش در‌آور** (ثبت آدرس + لوکیشن). ادمین/ویزیتور/پخش همیشه از **وب‌سایت** استفاده می‌کنند، هرگز از بات.
- **مبنای نهایی مبلغ سفارش = ثبت پخش** (نه ثبت اولیه‌ی ویزیتور). کمیسیون هر دو نفر از همین رقم حساب می‌شود.

## Business rules (خلاصه — جزئیات کامل در آرتیفکت)
- **در‌آور**: ثبت مغازه در بات (نام، آدرس، عکس، امتیاز ۱–۳، لوکیشن). ویرایش تا ۶ عصر (نرم، نه قفل سخت). افزودن اضطراری هر زمان → فوراً وارد لیست فردا. دستمزد ثابت/مغازه.
- **ویزیتور**: صبح پک رایگان تحویل می‌دهد (نظر+ویس+امتیاز ۱–۵). ۳ روز بعد پیگیری سفارش ۱ و ۲؛ تکرار بدون سقف تا خرید یا کنارگذاری دستی ادمین. دستمزد ثابت/مغازه + ٪۸ سفارش ۱و۲. اگر دو زیرتیم فعال باشد، لیست آدرس هر روز نصف‌نصف بین‌شان تقسیم می‌شود (به ترتیب ثبت، نه رندوم).
- **پخش**: بعد از بسته‌شدن سفارش توسط ویزیتور، محصول را می‌برد، تحویل می‌دهد، پول را می‌گیرد و ثبت می‌کند (مبنای نهایی مبلغ). بعد از ۲ خرید اول، سرکشی هفتگی (حضوری یا تلفنی؛ شماره را ادمین می‌دهد) با پخش است. دستمزد ٪۳ هر سفارش تحویل‌شده.
- **ادمین**: انتخاب ناحیه‌ی روزانه، تعیین نوع هر روز (پخش/پیگیری/تعطیل — قابل جابه‌جایی)، تنظیم آستانه‌ی امتیاز (فعلاً فقط ۳ ستاره)، تنظیم مبلغ‌ها/درصدها، فعال‌سازی زیرتیم دوم ویزیتور، کنارگذاری دستی مغازه از پیگیری، ترسیم/ویرایش مرز نواحی روی نقشه.
- **روز تعطیل**: کار آن روز زمان‌بندی نمی‌شود؛ کارهای معلق خودکار به روز کاری بعدی منتقل می‌شوند.
- **کاتالوگ محصول لازم نیست** — قیمت‌گذاری داخلی سیستم وجود ندارد؛ ویزیتور فقط نام محصول را ثبت می‌کند.

## Stack & layout
- `backend/` — FastAPI (API وب‌سایت) + aiogram v3 (بات در‌آور) + SQLAlchemy async + Alembic + PostgreSQL
- `webapp/` — HTML/CSS/JS خام (بدون بیلد) با Leaflet + OpenStreetMap برای ادمین/ویزیتور/پخش
- `deploy/Caddyfile` — پروکسی `/api` به بک‌اند + سرو استاتیک `webapp/` + SSL خودکار (Let's Encrypt)
- `scripts/backup.sh` / `scripts/restore.sh` — بکاپ خودکار هر ۶ ساعت به تلگرام ادمین + بازیابی سریع دستی
- `install.sh` — نصب تک‌خطی (Docker + کلون + پرسش تنظیمات + migrate + ساخت ادمین اول + کرون بکاپ)

## Design tokens (برای webapp، فاز ۳)
کاغذی روشن `#EEF0EA` / تیره `#11161A` · ویزیتور (سبزفیروزه‌ای) `#2F6F62` · پول/پخش (کهربایی) `#A9761F` · در‌آور (آبی‌سرمه‌ای) `#3E6491` · هشدار/باز `#AE4E37`. فونت: Vazirmatn (UI فارسی) + JetBrains Mono (اعداد/زمان/درصد).
